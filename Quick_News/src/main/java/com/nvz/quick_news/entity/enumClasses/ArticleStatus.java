package com.nvz.quick_news.entity.enumClasses;
/**
 * Enum class define a collection of constants (cannot assign a new value)
 * Each constant is an instance of the enum class
 */
public enum ArticleStatus {
    DRAFT, PUBLISHED, ARCHIVED
}
